// import { Router } from "express";
// import { addToFavorites, findByUserName } from "../controllers/favorites.controllers.js";
// import { authRequired } from "../middlewares/validateToken.js";

// const router = Router();

// router.post("favorites/add", authRequired, addToFavorites);

// router.get("favorites/find/:username", authRequired, findByUserName)

// export default router;